version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
  exclusions:
    paths:
      - pkg/apis/noobaa/v1alpha1
      - pkg/bundle
      - zz_generated\.go

run:
  timeout: 5m
