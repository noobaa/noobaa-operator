apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: noobaa
  name: vectors
spec:
  port:
    targetPort: vectors-https
  tls:
    termination: reencrypt
  to:
    kind: Service
    name: vectors
    weight: 100
  wildcardPolicy: None
