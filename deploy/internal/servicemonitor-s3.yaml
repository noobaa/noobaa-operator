apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: noobaa-s3-service-monitor
  labels:
    app: noobaa
spec:
  endpoints:
  - port: metrics-https
    path: /
    scheme: https
  namespaceSelector: {}
  selector:
    matchLabels:
      noobaa-s3-svc: "true"
